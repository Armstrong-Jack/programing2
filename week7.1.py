import tkinter as tk

root = tk.Tk()
root.title("Jack Armstrong")
root.geometry("600x200")
#you need the root.mainloop() because otherwise the program will end one it has loaded as the code will only loop once without it 
root.mainloop()
